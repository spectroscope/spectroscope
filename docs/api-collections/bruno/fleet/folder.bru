meta {
  name: fleet
  seq: 11
}

docs {
  The fleet roster, per-node replay, and node control (stop / gate / spawn). The hub is opt-in via SPECTRO_HUB_PORT; without it the roster answers enabled:false and node routes answer 404.
}
