# What install.sh generates on THIS machine. settings.yml carries a secret key
# created here, so it is a credential file and never leaves the machine — the
# same reason ./.env is ignored for the Langfuse stack next door.
searxng/
